
- 2026-05-27: The mocked live-data failure test on `/` produces a browser console `Failed to load resource: the server responded with a status of 503` error because the red-path intentionally fulfills `**/universe` with HTTP 503. The harness attaches that browser diagnostic for visibility but does not fail the fallback-path test on it.
- 2026-05-27: A direct manual browse of `http://127.0.0.1:8081/` still hits the real provision API and emits CORS errors in the browser console before the later web-hardening tasks are applied; this is an existing runtime blocker, not a harness regression.

- 2026-05-27: The root shell does not currently use the hydration-safe web color-scheme helper. `src/app/_layout.tsx:3,10-12` imports `useColorScheme` from `react-native` directly, while `src/hooks/use-color-scheme.web.ts:5-20` exists specifically to return `'light'` until client hydration completes.
- 2026-05-27: Mounting `RecordingProvider` in the root layout means recording side effects start at app-shell load, not only on `/record`. `_layout.tsx` mounts the provider for the whole shell (`src/app/_layout.tsx:13-16`), and `src/features/recording/recording-state.tsx:58-83` requests recording permission / audio mode setup in a mount effect.
- 2026-05-27: Record mode still mounts the shared `UniverseScreen` data effects even though most non-record chrome is hidden. `src/features/universe/universe-screen.tsx:405-475` always calls `loadUniverse()` and podcast hydration on mount, while `src/features/universe/universe-screen.tsx:1028-1133` conditionally hides universe-only chrome when `mode === 'record'`.
- 2026-05-27: The current web tab shell and icon surface rely on browser-only styling primitives. `src/components/app-tabs.web.tsx:87-90` uses `backdropFilter` / `WebkitBackdropFilter` with `@ts-expect-error`, and `src/components/animated-icon.web.tsx:65` renders a literal `<div>` styled via `src/components/animated-icon.module.css:1-6`.
## 2026-05-27 recording web risk notes

- No dedicated recording capability/helper file was found under `src/`; browser-sensitive behavior is embedded directly in `src/features/recording/recording-state.tsx` and `src/features/recording/ingest-client.ts`.
- `src/features/recording/recording-state.tsx:63`, `102`, `112-113`, and `296-300` assume Expo recorder permissions, recorder preparation, recorder stop, and URI emission all work uniformly on web.
- `src/features/recording/recording-state.tsx:75-77`, `115-117`, `125-127`, and `135-137` convert bootstrap/control failures into `console.warn` only, so unsupported-browser or denied-permission cases do not become explicit UI state.
- `src/features/recording/ingest-client.ts:24-34` assumes `expo-file-system` `File` can be constructed from the recorder URI and sent as multipart form data on web.
- No `TODO` / `FIXME` markers were present in `src/features/recording/*` during this inspection.
- 2026-05-28: Visible topic labels in the universe canvas are camera/layout-dependent on web, so Playwright should not treat them as the primary stable selector surface for parity tests. The explicit search-result buttons are the reliable route for browser automation and failure/recovery coverage.
- 2026-05-28: The web tab shell had a root-route parity bug because the `/` trigger was registered as `name="home"` instead of the actual `index` route name. URL navigation still worked, but the Universe tab focus state drifted on direct loads and history navigation until the trigger name was corrected.
- 2026-05-28: Chrome/Playwright emits intentional browser-network failures here with the full deployed endpoint URL plus a trailing `:: net::ERR_FAILED` (for example on `/universe`, `/podcasts`, and `/ingest`), while the explicit red-path `/universe` unmatched-route check emits a 404 `Failed to load resource` console error. Final regression assertions should match those concrete diagnostic shapes rather than assuming relative URLs or clean-console behavior on red paths.
